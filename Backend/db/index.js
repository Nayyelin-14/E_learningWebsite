const { users, emailVerification, Two_factor } = require("./Schemas/auth");
const { allcourses } = require("./Schemas/courses");

module.exports = {
  users,
  emailVerification,
  Two_factor,
  allcourses,
};
